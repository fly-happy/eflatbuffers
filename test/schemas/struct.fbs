enum Color : int { Red, Green, Blue }

struct Nest {
  age:int;
  color:Color;
}

struct Everything {
  nested:Nest;
  size:float;
  color:Color;
}

table MyTable {
  everything:Everything;
}

root_type MyTable;