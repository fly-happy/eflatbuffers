enum Color : byte { Red, Green, Blue }

struct Nest {
  age:int;
  color:Color;
}

struct Everything {
  size:float; 
  nested:Nest;
  color:Color;
}

table MyTable {
  everything:Everything;
}

root_type MyTable;